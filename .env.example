# ==============================================================================
# TEMPLATE - Copy this file to .env and replace placeholders with actual values
# NEVER commit .env file with real credentials!
# ==============================================================================

# Required: Anthropic API key for Claude AI features
# Get your key at: https://console.anthropic.com/
ANTHROPIC_API_KEY=your_api_key_here

# Optional: Claude model configuration
CLAUDE_MODEL=claude-sonnet-4-20250514
CLAUDE_MAX_TOKENS=4096

# Optional: Rate limiting (requests per minute)
YFINANCE_REQUESTS_PER_MINUTE=60
CLAUDE_REQUESTS_PER_MINUTE=50

# Optional: Portfolio database path
PORTFOLIO_DB_PATH=data/portfolio.db

# ==============================================================================
# Newsletter/Email Configuration (Optional - only needed for email features)
# ==============================================================================

# SMTP Server Configuration
# For Gmail, use: smtp.gmail.com (port 587) with an App Password
# Get Gmail App Password: https://myaccount.google.com/apppasswords
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587

# SECURITY WARNING: Use environment-specific credentials, never commit real values!
# Replace these placeholders with your actual credentials in .env (not here!)
SMTP_USERNAME=your_email@example.com
SMTP_PASSWORD=your_app_password_here

# Newsletter Settings
NEWSLETTER_SENDER_EMAIL=your_email@example.com
NEWSLETTER_SENDER_NAME=Portfolio Manager
NEWSLETTER_RECIPIENTS=recipient1@example.com,recipient2@example.com

# Newsletter Schedule (24-hour format HH:MM)
NEWSLETTER_SCHEDULE_MORNING=08:00
NEWSLETTER_SCHEDULE_EVENING=17:30
